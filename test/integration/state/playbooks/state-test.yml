- name: ensure existing resources are cleaned up
  hosts: localhost
  strategy: state
  connection: local
  state: absent
  enforcing: yes

  roles:
  - state-test

- name: run state test suite
  hosts: localhost
  strategy: state
  connection: local

  roles:
  - state-test
