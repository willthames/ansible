- name: Run state-test setup
  hosts: localhost
  strategy: state
  connection: local
  any_errors_fatal: yes

  roles:
  - state-test-setup

- name: Run state-test validation
  hosts: localhost
  strategy: linear
  connection: local
  any_errors_fatal: yes

  roles:
  - state-test-validate
